Route params

To make a route param OPTIONAL add a ?
Route::get('/blog/{id?}', [PostsController::class, 'show']);
-any $id will override default

-provide a default value inside controll method
public function show($id = 1)
    {
        return $id;
    }